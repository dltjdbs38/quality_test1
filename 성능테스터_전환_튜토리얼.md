# 성능 테스터 전환 튜토리얼 (Spring Boot 개발자용)

> 백엔드(Spring Boot/JWT/Redis/MySQL/Docker) 경험을 성능테스트 역량으로 연결하는 관점에서 정리했습니다.
> 왜 백엔드 경험이 유리한가? → 성능테스트는 결국 "코드가 아니라 시스템이 부하 아래서 어떻게 무너지는가"를 보는 일입니다.
> Connection Pool, Redis 캐시 히트율, JWT 검증 오버헤드 같은 걸 이미 만들어본 사람이 병목을 훨씬 빨리 짚어냅니다.

---

## 1부. 개념 잡기 (커리큘럼 1~3)

### 1-1. 성능이란?
성능은 "빠르다/느리다"가 아니라 **정량화된 지표**입니다.

| 지표 | 의미 | Spring Boot 관점 유사 개념 |
|---|---|---|
| TPS (Transactions Per Second) | 초당 처리 가능한 요청 수 | `@RestController` 엔드포인트가 초당 몇 번 호출을 받아내는가 |
| Response Time | 요청→응답까지 걸린 시간 (평균, p95, p99) | Controller 진입~응답까지의 latency |
| Throughput | 단위 시간당 처리된 데이터량 | - |
| Error Rate | 실패 요청 비율 (5xx, timeout) | Redis 커넥션 풀 고갈 시 발생하는 에러율과 동일 개념 |
| Concurrent Users | 동시 접속자 수 | Thread Group의 스레드 수와 직결 |

**SLA (Service Level Agreement)**: 고객과 계약된 약속 수준 (예: "p95 응답시간 500ms 이하")
**SLO (Service Level Objective)**: 팀 내부 목표치 (SLA보다 보수적으로 잡는 게 보통)

> 예시: "쇼핑몰 결제 API는 TPS 100, p95 응답시간 800ms, 에러율 0.1% 이하를 만족해야 한다" → 이게 지표정의서의 뼈대입니다.

### 1-2. 성능테스트란? 목적과 접근법
- **목적**: 장애 나기 *전에* 한계점을 찾는 것 (사후 대응이 아니라 사전 검증)
- **접근 방법 4가지**
  1. **부하테스트 (Load Test)**: 예상 트래픽만큼 부하를 주고 지표 확인
  2. **스트레스테스트 (Stress Test)**: 임계점까지 부하를 계속 올려서 "언제 터지는지" 확인 (커리큘럼의 case4가 이거)
  3. **내구성테스트 (Endurance/Soak Test)**: 장시간 부하를 유지해서 메모리 누수, 커넥션 누수 확인
  4. **스파이크테스트 (Spike Test)**: 순간적으로 트래픽 급증 시 반응 확인 (예: 선착순 이벤트)

### 1-3. 모니터링 도구 이해
- **Scouter**: APM(Application Performance Management) 도구. 트랜잭션 단위로 어디서 시간이 소요됐는지(SQL, 외부 API 호출, 내부 로직) 추적
- **Grafana**: 시계열 데이터(CPU, Memory, TPS 등)를 대시보드로 시각화. 보통 Prometheus/InfluxDB와 조합

---

## 2부. 환경 세팅 실습 (커리큘럼 4~5)

### 2-1. Scouter 설치 (Docker Compose 기준)
Scouter는 단일 이미지가 아니라 **서버(collector)**와 **웹 클라이언트(paper)** 두 컨테이너로 구성됩니다.
`docker network create`는 따로 안 해도 됩니다 — compose가 알아서 전용 네트워크를 만들어줍니다.

```yaml
# docker-compose.scouter.yml
version: '3.2'

services:
  scouter:
    image: scouterapm/scouter-server:2.17.1
    container_name: scouter-server
    environment:
      - SC_SERVER_ID=SCOUTER-COLLECTOR
      - NET_HTTP_SERVER_ENABLED=true
      - NET_HTTP_API_SWAGGER_ENABLED=true
      - NET_HTTP_API_ENABLED=true
      - MGR_PURGE_PROFILE_KEEP_DAYS=2
      - MGR_PURGE_XLOG_KEEP_DAYS=5
      - MGR_PURGE_COUNTER_KEEP_DAYS=15
      - JAVA_OPT=-Xms512m -Xmx512m
    volumes:
      - ./scouter-logs:/home/scouter-server/logs
      - ./scouter-data:/home/scouter-server/database
    ports:
      - "6188:6188"
      - "6180:6180"
      - "6100:6100"
      - "6100:6100/udp"

  paper:
    image: scouterapm/scouter-paper:2.6.5-snapshot
    container_name: scouter-paper
    restart: always
    ports:
      - "8080:80"
```

```bash
mkdir -p scouter-logs scouter-data
docker compose -f docker-compose.scouter.yml up -d
```

- **Scouter 웹 UI(Paper) 접속**: `http://localhost:8080`
- **Server(Collector) 포트**: `6100` (Agent가 이 포트로 데이터를 전송)

> 예전에는 Scouter Client라는 Java 기반 데스크톱 앱으로 접속하는 방식이 일반적이었는데, 지금은 Paper(웹 UI)로 브라우저에서 바로 보는 게 더 간편합니다. 데스크톱 클라이언트를 요구하는 강의라면 별도로 `scouter-client`를 받아서 6100 포트로 접속하면 됩니다.

### 2-2. Java 애플리케이션에 Scouter Agent 붙이기
Spring Boot 앱 실행 시 java agent로 붙이는 방식이라 별도 코드 수정 불필요:
```bash
java -javaagent:/path/to/scouter.agent.jar \
  -Dobj_name=my-spring-app \
  -Dscouter.config=/path/to/scouter.conf \
  -jar my-app.jar
```

### 2-3. Grafana + Prometheus (JMeter 결과 시각화용)
```yaml
# docker-compose.yml
version: '3'
services:
  prometheus:
    image: prom/prometheus
    ports: ["9090:9090"]
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
  grafana:
    image: grafana/grafana
    ports: ["3000:3000"]
    depends_on: [prometheus]
```
```bash
docker compose up -d
# Grafana 접속: http://localhost:3000 (기본 계정 admin/admin)
```

### 2-4. JMeter 설치
```bash
# macOS
brew install jmeter

# Ubuntu/Debian
sudo apt update
sudo apt install openjdk-17-jdk -y   # JMeter는 Java 기반, JDK 11+ 필요
wget https://dlcdn.apache.org/jmeter/binaries/apache-jmeter-5.6.3.tgz
tar -xvzf apache-jmeter-5.6.3.tgz
cd apache-jmeter-5.6.3/bin
./jmeter    # GUI 모드 실행

# 설치 확인
jmeter --version
```
> 최신 버전은 https://jmeter.apache.org/download_jmeter.cgi 에서 확인하세요 (버전은 계속 업데이트됩니다).

### 2-5. curl로 워밍업 테스트
JMeter 스크립트 짜기 전에, 대상 API가 정상 응답하는지 curl로 먼저 확인하는 게 습관이 되어야 합니다.
아래는 연습용으로 만든 `szg-shop` 목업 서버(`node server.js`, localhost:3000) 기준입니다.

```bash
# 단순 GET (상품 목록)
curl -i "http://localhost:3000/api/products?page=1&size=3"

# 응답 시간만 측정
curl -o /dev/null -s -w "DNS: %{time_namelookup}s, Connect: %{time_connect}s, Total: %{time_total}s\n" \
  http://localhost:3000/api/products

# 로그인 API (JWT 발급 확인 - 본인 백엔드 경험과 직결되는 부분)
curl -X POST http://localhost:3000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test1234"}'

# 발급된 토큰으로 인증 필요한 API 호출
curl -H "Authorization: Bearer <TOKEN>" http://localhost:3000/api/orders -X POST
```
> 실제 강의에서 별도의 SZG 쇼핑몰 서버(사내/교육용 URL)를 제공한다면 그 주소로 바꿔서 그대로 사용하면 됩니다.

---

## 3부. JMeter 스크립트 작성 (커리큘럼 6~7)

### 3-1. Recording Tool 세팅
JMeter는 브라우저 프록시로 동작을 녹화해서 스크립트를 자동 생성할 수 있습니다.

1. JMeter GUI → Test Plan 우클릭 → Add → Threads → Thread Group
2. Thread Group 우클릭 → Add → Config Element → **HTTP Request Defaults** (도메인/포트 공통 설정)
3. Test Plan 우클릭 → Add → Non-Test Elements → **HTTP(S) Test Script Recorder**
   - Port: 8888 (기본값)
   - Target Controller: Test Plan > Thread Group > Recording Controller 지정
4. 브라우저 프록시를 127.0.0.1:8888로 설정 (또는 JMeter가 제공하는 인증서 설치 후 HTTPS 녹화)
5. Recorder에서 **Start** 클릭 → 브라우저로 실제 쇼핑몰(SZG) 시나리오 수행 (로그인 → 상품 조회 → 장바구니 → 결제)
6. **Stop** → Recording Controller 아래 요청들이 자동 생성됨

### 3-2. 필수 구성요소 이해
| 구성요소 | 역할 |
|---|---|
| **Thread Group** | 가상 사용자 수, Ramp-up 시간, 반복 횟수 정의 |
| **HTTP Cookie Manager** | 세션 쿠키 자동 관리 (로그인 유지) |
| **HTTP Cache Manager** | 브라우저 캐시 시뮬레이션 (정적 리소스 재요청 방지) |
| **Recording Controller** | 녹화된 요청들이 저장되는 컨테이너 |
| **Listener (View Results Tree)** | 요청/응답 상세 확인 (디버깅용, 부하테스트 시엔 끄기) |
| **Listener (Summary Report/Aggregate Report)** | TPS, 평균/최대/최소 응답시간, 에러율 집계 |

### 3-3. Correlation (상관관계) 처리 — 가장 중요한 개념
녹화된 스크립트를 그대로 재생하면 실패합니다. 로그인 시 서버가 발급한 **동적 값**(세션ID, JWT 토큰, CSRF 토큰)이 녹화 시점 값으로 하드코딩되어 있기 때문입니다.

> 이건 본인이 JWT 발급/검증 로직을 직접 만들어봤으니 개념적으로 바로 이해될 겁니다: "로그인 응답의 토큰을 다음 요청 헤더에 넣어야 한다"는 걸 JMeter가 자동으로 하게 만드는 작업입니다.

**JSON Extractor로 토큰 추출:**
1. 로그인 요청(HTTP Request) 우클릭 → Add → Post Processors → **JSON Extractor**
2. 설정:
   - Variable name: `authToken`
   - JSON Path: `$.token`
3. 이후 요청의 헤더에서 `${authToken}` 변수로 참조:
   ```
   Authorization: Bearer ${authToken}
   ```

**정규식으로 추출 (Regular Expression Extractor):**
- HTML 응답에서 CSRF 토큰 등 추출할 때 사용
- Reference Name: `csrfToken`, Regular Expression: `name="csrf" value="(.+?)"`

### 3-4. Scouter로 트랜잭션 확인
JMeter로 부하를 주는 동안 Scouter 웹 UI(`http://localhost:8080`, Paper)에서:
- **XLog 탭**: 각 트랜잭션(요청)이 몇 ms 걸렸는지, 어느 서버 인스턴스가 처리했는지
- **Profile 탭**: 트랜잭션 내부에서 SQL 실행 시간, 외부 API 호출 시간 breakdown
- Correlation 테스트: JMeter 요청 하나가 Scouter에서 실제로 어떤 트랜잭션으로 잡히는지 매칭 확인 (요청 시각 + URL로 대조)

> 참고: `szg-shop` 목업 서버는 Node.js라서 Scouter Java Agent를 붙일 수 없습니다(Scouter는 Java 기반 앱 전용). Scouter 실습 자체는 강의에서 제공하는 실제 Java(Spring Boot 등) 대상 서버로 진행하고, `szg-shop`은 JMeter 스크립트 작성/Correlation/Connection Pool 병목 개념을 손에 익히는 용도로 쓰시는 걸 추천합니다.

---

## 4부. 도구 비교 (커리큘럼 8)

| 도구 | 특징 | 언제 쓰나 |
|---|---|---|
| **JMeter** | GUI 기반, Java, 플러그인 풍부, 배우기 쉬움 | 표준적인 부하테스트, 초보자 |
| **K6** | JS 기반 스크립트, 코드로 관리(Git 버전관리 용이), 경량 | CI/CD 파이프라인에 통합할 때 |
| **Gatling** | Scala 기반, 고성능(적은 리소스로 대량 부하), 리포트 예쁨 | 대규모 부하, 리소스 제약 환경 |
| **LoadRunner** | 상용, 프로토콜 지원 매우 넓음(엔터프라이즈) | 레거시/특수 프로토콜 환경 |

**Observability 도구 비교 (Scouter 외)**
- **Pinpoint**: 네이버 오픈소스 APM, 분산 트레이싱 강점
- **Datadog / New Relic**: 상용 SaaS, 올인원(로그+메트릭+APM)
- **Prometheus + Grafana**: 메트릭 수집·시각화 표준 조합, APM은 아니고 인프라/메트릭 중심

```bash
# k6 간단 실습 (참고용 - 원한다면 이 커리큘럼 후 추가 학습 추천)
brew install k6
k6 run script.js
```

---

## 5부. 실전 부하테스트 (커리큘럼 9~13, SZG 쇼핑몰 case 1~4)

### 5-1. 대상화면 선정 & 목표 설정
- 우선순위: **트래픽이 많은 화면** (메인, 상품 목록, 검색) > **비즈니스 크리티컬** (결제, 로그인) > 기타
- 목표 설정 예시:
  ```
  대상: 상품 목록 조회 API
  목표 TPS: 200
  목표 응답시간: p95 < 1초
  목표 동시사용자: 500명
  ```

### 5-2. Thread Group 설정 예시 (부하 투입)
```
Number of Threads (users): 500
Ramp-up period: 60 (초 - 500명이 60초에 걸쳐 서서히 증가)
Loop Count: 10 (또는 Duration 기반으로 설정)
```

### 5-3. CLI(Non-GUI)로 실행 — 실제 부하테스트는 GUI가 아니라 CLI로 돌립니다
GUI는 리소스를 많이 먹어서 결과 왜곡이 생깁니다. 반드시 아래처럼 실행:
```bash
jmeter -n -t szg_shopping.jmx -l result_case1.jtl -e -o report_case1/

# -n : non-GUI 모드
# -t : 테스트 스크립트(jmx) 지정
# -l : 결과 로그 파일(jtl)
# -e -o : 실행 후 HTML 대시보드 리포트 자동 생성 (report_case1 폴더)
```

원격/서버에서 백그라운드로 오래 돌릴 때:
```bash
nohup jmeter -n -t szg_shopping.jmx -l result.jtl > jmeter.log 2>&1 &

# 진행 상황 실시간 확인
tail -f jmeter.log
```

### 5-4. Case별 흐름 (커리큘럼 설계 그대로 따라가기)
| Case | 목적 | 확인할 것 |
|---|---|---|
| Case 1 | 기본 부하 (목표치 수준) | 목표 TPS/응답시간 충족 여부, 에러율 |
| Case 2 | 부하 상향 조정 | Case1 대비 병목 위치 변화 |
| Case 3 | 추가 상향 | 지속적 병목 vs 새로운 병목 |
| Case 4 | 임계점까지 부하 증가 (Stress Test) | 시스템이 "언제, 왜" 무너지는지 |

각 Case 후 **Scouter/Grafana에서 확인할 지표**:
- WAS: CPU, Heap Memory, GC 횟수/시간, Active Thread 수
- **DB Connection Pool**: 활성 커넥션 수, 대기 중인 커넥션 수 (⚠️ 아래 참고)
- Network: 대역폭 사용률, TCP 커넥션 수
- Middleware(Redis 등): 커넥션 풀, 캐시 히트율, evict 발생 여부

### 5-5. DB Connection Pool 개념 (Spring Boot 경험과 바로 연결됨)
Spring Boot에서 HikariCP를 써보셨으니 익숙한 개념일 겁니다:
- **Connection Pool 크기가 작으면**: 요청이 몰릴 때 커넥션을 기다리는 대기(wait) 발생 → 응답시간 급증
- **Connection Pool 크기가 너무 크면**: DB 서버 자체가 커넥션 관리 오버헤드로 느려짐
- 부하테스트에서 흔한 패턴: TPS를 올리다 보면 어느 지점에서 응답시간이 갑자기 튀는데, 그 지점이 보통 **Connection Pool 고갈 시점**과 일치합니다. 이게 "병목구간 확인"의 대표 사례입니다.

```yaml
# application.yml (HikariCP 예시 - 본인이 이미 알고 있는 설정)
spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 3000
```
→ 부하테스트로 `maximum-pool-size`를 20 → 50으로 튜닝했을 때 TPS/응답시간이 어떻게 바뀌는지가 "전후 성능비교" 리포트의 핵심 소스가 됩니다.

---

## 6부. 리포트 작성 & 튜닝 (커리큘럼 14~15)

### 6-1. 성능테스트 리포트 구성
1. **테스트 개요**: 대상, 목표(SLA/SLO), 테스트 환경(서버 스펙, 네트워크)
2. **테스트 시나리오**: 시나리오별 사용자 수, Ramp-up, 지속시간
3. **결과 요약**: TPS, 응답시간(평균/p95/p99), 에러율 (표+그래프)
4. **병목 분석**: 구간별(WAS/DB/Middleware/Network) 병목 지점과 근거 데이터
5. **튜닝 내역 및 전후 비교**: 원인 → 조치 → 재테스트 결과
6. **결론 및 권고사항**

### 6-2. 원인-대응 매핑 예시 (실제로 자주 나오는 패턴)
| 증상 | 의심되는 원인 | 대응 |
|---|---|---|
| 특정 TPS 이상에서 응답시간 급증 | DB Connection Pool 고갈 | Pool 크기 조정, 슬로우 쿼리 최적화 |
| CPU는 여유 있는데 응답 느림 | 스레드 대기(Lock), 외부 API 호출 지연 | 비동기 처리, 타임아웃 조정 |
| 장시간 테스트 후 응답시간 점진적 증가 | 메모리 누수, GC 압박 | Heap 덤프 분석, 캐시 만료 정책 점검 |
| 초반엔 빠르다가 특정 시점 급락 | 캐시(Redis) 워밍업 안됨/evict 발생 | 캐시 사전 워밍업, TTL/메모리 정책 조정 |

### 6-3. JMeter 결과 파일(jtl) 분석 커맨드
```bash
# jtl 결과에서 에러 건만 필터링
grep -c "false" result_case1.jtl   # success="false" 카운트 (대략적 확인용)

# CLI 리포트 재생성 (이미 있는 jtl로 대시보드만 다시 만들 때)
jmeter -g result_case1.jtl -o report_case1_v2/
```

---

## 학습 순서 요약 (체크리스트)

- [ ] 1~3부: 개념 정리 (하루면 충분)
- [ ] Docker로 Scouter + Grafana/Prometheus 환경 구축
- [ ] JMeter 설치 후 curl로 대상 API 확인
- [ ] Recording Tool로 로그인→조회→결제 시나리오 녹화
- [ ] JSON/정규식 Extractor로 correlation 처리
- [ ] Non-GUI 모드로 Case1~4 부하 실행
- [ ] Scouter/Grafana로 병목 구간 특정 (특히 DB Connection Pool)
- [ ] 튜닝 적용 → 재테스트 → 전후 비교 리포트 작성

막히는 실습 단계 있으면 그 부분만 더 깊게 풀어드릴게요 (예: JSON Extractor 실제 예제, Grafana 대시보드 패널 구성 등).
